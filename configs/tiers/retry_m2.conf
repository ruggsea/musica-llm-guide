# Retry: MiniMax-M2 only (M2.1 and Kimi-Linear already PASS)
# Previous failure: FlashInfer ninja build race condition
# Format: model_id|dtype|tp|extra_args
TIER_NAME="retry_m2"
TIER_GPUS=4
MODELS=(
"MiniMaxAI/MiniMax-M2|auto|4|--trust-remote-code"
)
