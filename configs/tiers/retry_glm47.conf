# Retry: GLM-4.7-Flash (previously failed: glm4_moe_lite arch not in transformers 4.57.6)
# Now fixed: transformers upgraded to 5.3.0 which has glm4_moe_lite
# GLM-4.7-Flash: 30B MoE, FP8 variant cached as zai-org/GLM-4.7-Flash
# Memory: 30B FP8 * 1.15 = ~35 GB -> TP=2: ~17 GB/GPU (fits easily)
# Format: model_id|dtype|tp|extra_args
TIER_NAME="retry_glm47"
TIER_GPUS=4
MODELS=(
"zai-org/GLM-4.7-Flash|auto|2|--trust-remote-code"
)
