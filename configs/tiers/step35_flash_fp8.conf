# Step-3.5-Flash FP8 -- single node TP=2
# 199B MoE (11B active), ships FP8, Apache 2.0 (StepFun)
# TP=4 FAILS: gate/up weight size 320 not divisible by FP8 block_n=128
# TP=2: output_size=640, 640/128=5 -- works
# Memory: ~200 GB FP8 -> TP=2: ~100 GB/GPU (tight on 94GB H100!)
# Actually MoE: experts split across TP, ~100 GB/GPU may be too much
# Try with gpu_memory_utilization=0.95
# Needs --trust-remote-code (custom step3p5 architecture)
# Format: model_id|dtype|tp|extra_args
TIER_NAME="step35_flash_fp8"
TIER_GPUS=4
MODELS=(
"stepfun-ai/Step-3.5-Flash-FP8|auto|2|--trust-remote-code"
)
